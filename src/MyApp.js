import React from 'react'
import {View ,Texd} from 'react-native'
// import console = require('console');

const Card =(props) => {
    console.log("==>",props);
    const {person} = props;
    const {textStyles,containerStyle} =styles;

  

}
